# Copy to android/keystore.properties for LOCAL release builds (git-ignored).
# CI does not use this file — it reads the same values from repo secrets.
storeFile=/absolute/path/to/upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
